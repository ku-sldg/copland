This directory contains the Coq sources for proofs about Attestation
Protocol Description Terms (APDTs) and their semantics.

To build the proofs and an HTML document, type:

    $ make

and then view html/toc.html.

To build the PDF document, type:

    $ ./mkdoc

and then view apdtcoq.pdf.
